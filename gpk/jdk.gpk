-- CMake gpk script

-- Description of the gpk
description = [[
]]

-- Definition section
definition = {
   pkgname = "jdk",
   pkggroup = "core",
   pkgfamily = "jdk",
}

-- Required types
prerequisite = {}

-- Build section
build = {
   -- Source of package
   -- Build command
   command = [[
      cp -r * <pkginstall>/.
   ]]
}

-- Lmod section
lmod = {
   help = [[This module loads Java Development Kit (JDK) v. <pkgversion>.]],
}

--[[
# vi:syntax=lua
--]]
